# Cloud MySQL (e.g. PlanetScale, AWS RDS, Azure MySQL)
# Set env: SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD
# Or use this file with profile: spring.profiles.active=cloud

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
