# Production profile: use with spring.profiles.active=prod
# Set SUPABASE_DB_URL, SUPABASE_DB_PASSWORD (and optionally SERVER_PORT, APP_JWT_SECRET) via environment
server.port=${PORT:${SERVER_PORT:8080}}
# H2 console is disabled by default; only enabled when profile local/dev/h2 is active (see SecurityConfig)
